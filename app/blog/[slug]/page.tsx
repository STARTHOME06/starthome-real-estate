import Link from "next/link";
import { notFound } from "next/navigation";
import { CTA } from "@/components/CTA";
const posts:Record<string,{title:string;intro:string}>={ "vendere-casa":{title:"Come preparare casa per una vendita più efficace",intro:"Presentare bene un immobile non significa nasconderne i difetti: significa permettere a chi visita di comprenderne subito il potenziale."},"valutazione-immobiliare":{title:"Valutazione immobiliare: prezzo richiesto e valore di mercato",intro:"Il valore non nasce da un algoritmo isolato, ma dall’incontro tra dati, caratteristiche dell’immobile e domanda reale nella microzona."},"comprare-casa":{title:"Comprare casa: i controlli da fare prima della proposta",intro:"Una proposta consapevole parte dalla verifica di documenti, conformità, costi e condizioni sospensive."}};
export function generateStaticParams(){return Object.keys(posts).map(slug=>({slug}));}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=posts[slug];if(!p)notFound();return <><article className="py-20"><div className="mx-auto max-w-3xl px-5"><Link href="/blog" className="text-xs font-bold uppercase tracking-widest text-gold">← Tutte le guide</Link><h1 className="display-title mt-8">{p.title}</h1><p className="mt-8 text-xl leading-9 text-ink/65">{p.intro}</p><div className="mt-12 space-y-7 leading-8 text-ink/70"><h2 className="font-serif text-3xl font-semibold text-ink">Partire dai dati giusti</h2><p>Ogni immobile è unico, ma il mercato offre riferimenti concreti. Analizzare vendite comparabili, tempi medi e domanda attiva permette di prendere decisioni più solide.</p><h2 className="font-serif text-3xl font-semibold text-ink">La strategia conta</h2><p>Prezzo, presentazione, documentazione e promozione devono lavorare insieme. Un consulente locale coordina questi elementi e rende il percorso più semplice, trasparente e prevedibile.</p></div></div></article><CTA/></>;}
