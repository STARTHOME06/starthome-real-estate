import type { Metadata } from "next";
import { LeadForm } from "@/components/Forms";
import { site, whatsappUrl } from "@/lib/site";

export const metadata: Metadata = { title: "Contatti", description: `Contatta ${site.name} per vendere, affittare, acquistare o valutare un immobile.` };
export default function Page() { return <><section className="bg-ink py-20 text-white"><div className="container-site"><p className="eyebrow">Parliamone</p><h1 className="display-title">Il tuo progetto immobiliare<br/>inizia da qui.</h1></div></section><section className="py-20"><div className="container-site grid gap-14 lg:grid-cols-[.7fr_1.3fr]"><div><h2 className="font-serif text-3xl font-semibold">{site.name}</h2><div className="mt-7 space-y-5 text-sm leading-7 text-ink/60"><p><strong className="text-ink">Sede</strong><br/>{site.postalAddress}</p><p><strong className="text-ink">Cellulare</strong><br/><a href={`tel:${site.phoneHref}`}>{site.phoneDisplay}</a><br/><a className="font-semibold text-gold" href={whatsappUrl("Buongiorno STARTHOME REAL ESTATE, vorrei ricevere informazioni.")} target="_blank" rel="noreferrer">Contattaci su WhatsApp</a></p><p><strong className="text-ink">Email</strong><br/><a href={`mailto:${site.email}`}>{site.email}</a></p><p><strong className="text-ink">Orari</strong><br/>{site.hours.weekdays}<br/>{site.hours.saturday}</p><p><strong className="text-ink">P.IVA</strong><br/>{site.vatNumber}</p></div></div><div className="bg-sand p-7 sm:p-10"><h2 className="font-serif text-3xl font-semibold">Come possiamo aiutarti?</h2><p className="mb-7 mt-2 text-sm text-ink/55">Raccontaci la tua esigenza. Ti risponderemo con indicazioni concrete.</p><LeadForm/></div></div></section></>; }
